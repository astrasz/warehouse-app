{% extends 'base.html.twig' %}

{% block content %}
<div class="d-flex flex-column my-auto" style="height: 75vh;">
	<div class="row justify-content-center align-items-center g-0 h-100">
        <div class="chart-container" style="height:35vw; width:70vw; min-heigth: 350px; min-width: 700px">
            {{ render_chart(chart) }}
        </div>
    </div>
</div>
{% endblock %}
